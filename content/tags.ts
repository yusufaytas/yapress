import type { TagDefinition } from "@/types/content";

/**
 * Tag Registry
 * 
 * Optional: Define tags with custom titles and descriptions.
 * Tags not listed here will still work but use auto-generated titles.
 * 
 * Example:
 * {
 *   slug: "typescript",
 *   title: "TypeScript",
 *   description: "Type-safe JavaScript development"
 * }
 */
const tags: TagDefinition[] = [];

export default tags;
